const Tree = require("./bst");
const prettyPrint = require("./utils");

const newTree = new Tree();
const myTree = newTree.buildArray([1, 7, 4, 23, 8, 9, 4, 3, 5, 7, 9, 67, 6345, 324]);
// prettyPrint(myTree);
